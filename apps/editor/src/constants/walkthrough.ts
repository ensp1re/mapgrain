/** Base dwell on a step before the walkthrough advances, in milliseconds. */
export const WALK_STEP_MS = 1600;
export const WALK_SPEEDS: readonly number[] = [1, 1.5, 2, 0.5];
export const DEFAULT_WALK_SPEED = 1;
/** Below this the canvas cannot hold a state and its moves at a readable zoom. */
export const RUN_MOVES_IN_FRAME_WIDTH = 720;
