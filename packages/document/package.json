{
  "name": "@mapgrain/document",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate:schema": "node --experimental-strip-types scripts/write-json-schema.ts",
    "test": "node --test --experimental-strip-types --test-reporter=spec tests/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.41"
  }
}
