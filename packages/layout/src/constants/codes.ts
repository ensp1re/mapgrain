export const LAYOUT_CONFLICT_CODE = {
  INSUFFICIENT_SPACE: "insufficient_space",
} as const;

export const LAYOUT_STATUS = {
  LAID_OUT: "laid_out",
  CONFLICT: "conflict",
  SUPERSEDED: "superseded",
  INVALID: "invalid",
} as const;
